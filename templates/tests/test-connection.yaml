apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "HelloWorldGitOpsInfra.fullname" . }}-test-connection"
  labels:
    {{- include "HelloWorldGitOpsInfra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "HelloWorldGitOpsInfra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
