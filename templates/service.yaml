apiVersion: v1
kind: Service
metadata:
  name: {{ include "HelloWorldGitOpsInfra.fullname" . }}
  labels:
    {{- include "HelloWorldGitOpsInfra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "HelloWorldGitOpsInfra.selectorLabels" . | nindent 4 }}
